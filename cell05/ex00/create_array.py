#!/usr/bin/env python3
# The Shebang line makes the script directly executable.

# Define a list (Python's equivalent of an array) of numbers.
# I used the numbers visible in the example output: [2, 8, 9, 48, 8, 22, -12, 2]
array_of_numbers = [2, 8, 9, 48, 8, 22, -12, 2]

# Display the array. The print() function naturally handles the list format.
print(array_of_numbers)